const form = document.querySelector('#add-ex'),
  btn = document.querySelector('#submit-ex'),
  uidField = document.querySelector('#uid');

const defaultAction = '/api/exercise/add';

// Не закончено. Может и не надо вовсе.
